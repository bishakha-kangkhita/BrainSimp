# MySQL connection settings
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=brainsimp
DB_PORT=3306

# Server port (already using 3000, but good to have)
PORT=3000

# Later we'll add JWT secret here
JWT_SECRET=kangkhita
