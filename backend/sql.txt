-- 2. Create users table (fixed & complete version)
CREATE TABLE users (
    user_id         INT PRIMARY KEY AUTO_INCREMENT,
    username        VARCHAR(50) UNIQUE NOT NULL,
    email           VARCHAR(100) UNIQUE NOT NULL,
    password_hash   VARCHAR(255) NOT NULL,
    avatar          VARCHAR(50) DEFAULT 'default.png',
    level           INT DEFAULT 1,
    total_xp        INT DEFAULT 0,
    total_points    INT DEFAULT 0,
    games_played    INT DEFAULT 0,
    games_won       INT DEFAULT 0,
    perfect_scores  INT DEFAULT 0,
    join_date       TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    last_login      TIMESTAMP NULL DEFAULT NULL,
    theme           VARCHAR(20) DEFAULT 'light',
    sound_enabled   BOOLEAN DEFAULT TRUE,

    INDEX idx_username     (username),
    INDEX idx_level        (level),
    INDEX idx_total_points (total_points)
);
CREATE TABLE IF NOT EXISTS questions (
    id INT AUTO_INCREMENT PRIMARY KEY,
    category VARCHAR(50) NOT NULL,
    question_text TEXT NOT NULL,
    option_a VARCHAR(255) NOT NULL,
    option_b VARCHAR(255) NOT NULL,
    option_c VARCHAR(255) NOT NULL,
    option_d VARCHAR(255) NOT NULL,
    correct_answer CHAR(1) NOT NULL,
    difficulty VARCHAR(20) DEFAULT 'medium',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
INSERT INTO questions (category, question_text, option_a, option_b, option_c, option_d, correct_answer) VALUES
('trivia', 'What is the capital of Bangladesh?', 'Dhaka', 'Chittagong', 'Sylhet', 'Khulna', 'A'),
('trivia', 'Which planet is known as the Red Planet?', 'Venus', 'Mars', 'Jupiter', 'Saturn', 'B'),
('trivia', 'Who is the founder of Microsoft?', 'Steve Jobs', 'Bill Gates', 'Mark Zuckerberg', 'Elon Musk', 'B'),
('trivia', 'What is the largest ocean in the world?', 'Atlantic Ocean', 'Indian Ocean', 'Pacific Ocean', 'Arctic Ocean', 'C'),
('trivia', 'How many continents are there?', '5', '6', '7', '8', 'C'),
('trivia', 'What is the national flower of Bangladesh?', 'Water Lily', 'Rose', 'Lotus', 'Sunflower', 'A');